你是工业品牌短视频导演。请严格输出 **YAML**，结构必须符合下方模板字段，不要输出解释文字。

硬要求：
1) 每个 beat 必须同时给出：
   - vo：旁白（更口语、更自然、有连接词）
   - subtitle：字幕（更短但保留完整语义，约为 vo 的 70%-85% 信息量，不要变成广告标语）
2) beats 必须包含这四个 purpose（顺序固定）：
   - establish_context
   - show_capability
   - build_trust
   - brand_close
3) 每个 beat 给出 beat_structure（使用 wide/medium/detail/hero），遵循“同类衔接或越级衔接更舒服”的规律。
4) tags 必须用项目词典中的语义：factory/line/automation/testing/building/hero 等。

输出 YAML 模板（请填充）：
（粘贴 src/shooting/spec/creative_script_template.yaml 的结构）